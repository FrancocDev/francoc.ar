/** @type {import('next').NextConfig} */
const nextConfig = {
    images:{
        remotePatterns: [
            {
                hostname: 'porfolio.dev'
            }
        ] 
    }
};

export default nextConfig;
